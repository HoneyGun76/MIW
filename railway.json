{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "./start.sh",
    "healthcheckPath": "/miw/health.php",
    "healthcheckTimeout": 100
  }
}
